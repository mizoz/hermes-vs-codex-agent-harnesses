# Figures

Source diagrams are stored as Mermaid where possible. Export SVG/PNG only after
the text has stabilized.

Required figures:

1. architecture comparison;
2. task lifecycle;
3. benchmark pipeline;
4. queueing model.

Every figure needs alt text before publication.
