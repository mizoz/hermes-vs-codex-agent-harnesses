# Environment

Draft environment category observed on 2026-05-10:

- Controller-class Linux workstation.
- Optional worker-class node.
- Codex CLI available locally.
- Hermes-style operator-fleet wrapper available locally.
- Public benchmark mode must use synthetic fixtures only.

Do not publish local absolute paths, machine-specific secrets, private hostnames,
private records, message data, or private entity names in public
benchmark data.
